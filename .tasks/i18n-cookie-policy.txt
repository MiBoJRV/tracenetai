Опис завдання:
Додати мультимовність (EN + FR) до сторінки `cookie-policy.html`, використовуючи існуючу систему `data-i18n` + `i18n/en.json`/`i18n/fr.json` + `scripts/i18n.js`. Потрібно охопити шапку, мобільне меню, усі текстові секції політики, футер і cookie-banner.

План реалізації:
1) Проаналізувати HTML-структуру `cookie-policy.html`, виписати всі текстові елементи (заголовки, параграфи, списки, таблиці, CTA) та визначити ключі для перекладу (`cookiePolicy.overview`, `cookiePolicy.types.necessary`, тощо).
2) Проставити `data-i18n` або `data-i18n-html="true"` для всіх текстових блоків; у шапці, мобільному меню, футері й cookie-bannerі використати вже існуючі ключі.
3) Додати (якщо потрібно) дропдаун вибору мов у шапку/мобільне меню і підключити `scripts/i18n.js` наприкінці сторінки (за аналогією з іншими сторінками).
4) Розширити `i18n/en.json` і `i18n/fr.json` розділом `cookiePolicy.*`, де:
   - EN варіант містить поточний текст сторінки.
   - FR варіант — базовий переклад (можна уточнити пізніше).
5) Перевірити вручну, що перемикач мов працює на сторінці, і fallback на англійську зберігається.

Після завершення: оновити цей файл (що зроблено / що залишилось).
Опис завдання:
Додати мультимовність (EN + FR) до сторінки `cookie-policy.html`, використовуючи існуючу схему `data-i18n` + `i18n/en.json` & `i18n/fr.json` + `scripts/i18n.js`. Необхідно охопити шапку, мобільне меню, всі блоки політики (заголовки, описи, списки, таблиці, CTA), футер і cookie-banner, забезпечивши коректну роботу перемикача мов.

План реалізації:
1) Проаналізувати HTML-структуру `cookie-policy.html`, виписати всі текстові елементи, визначити ключі для `data-i18n` (наприклад, `cookiePolicy.overview`, `cookiePolicy.types.functional` тощо).
2) Проставити `data-i18n` / `data-i18n-html` для всіх текстів сторінки. Для шапки, мобільного меню, футера та cookie-bannerу використати існуючі глобальні ключі.
3) Переконатися, що сторінка має дропдаун мов (desktop + mobile) та підключений `scripts/i18n.js`.
4) Розширити `i18n/en.json` і `i18n/fr.json` новими ключами `cookiePolicy.*`, заповнивши англійський контент з поточної сторінки та додавши французький переклад.
5) Перевірити перемикання мов на сторінці, забезпечити fallback на англійську в разі відсутності ключа.

Після завершення: оновити цей файл, описавши що зроблено/не зроблено.

Статус виконання:
- Зроблено: `cookie-policy.html` повністю розмічено `data-i18n`, додано дропдаун мов (desktop + mobile) та підключено `scripts/i18n.js`; футер і cookie-banner використовують глобальні ключі; у `i18n/en.json` та `i18n/fr.json` з’явився розділ `cookiePolicy.*` із відповідними перекладами.
- Не зроблено: окремі додаткові сторінки (наприклад, terms-of-use) ще без мультимовності — можна винести в наступні задачі за потреби.

